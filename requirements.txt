# Dependencies are declared in pyproject.toml (the single source of truth).
#
#   Runtime install:      pip install .
#   With WIP scrobbling:  pip install .[scrobbling]
#   Development:          pip install -e .[dev]
#
# This file mirrors the runtime dependencies for tooling that expects it.
requests>=2.32.3
pillow>=10.4.0
cryptography>=41.0.0
keyring>=24.0.0
psutil>=5.9.0
